spring:
  application:
    name: anime-service
  threads:
    virtual:
      enabled: true
server:
  servlet:
    context-path: /${spring.application.name}
  error:
    include-stacktrace: on_param
  tomcat:
    threads:
      max: 20

database:
  url: localhost:3306
  username: testeMysql
  password: teste123